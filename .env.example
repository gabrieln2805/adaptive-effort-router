# Copy this file to .env and fill in your real key.
# .env is gitignored — never commit real API keys.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional — port the proxy listens on (defaults to 8787)
PORT=8787
